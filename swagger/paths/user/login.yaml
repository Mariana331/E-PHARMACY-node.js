tags:
  - User
summary: Login User
operationId: loginUser
description: Authenticate User and create session
security: []

requestBody:
  required: true
  content:
    application/json:
      schema:
        $ref: '../../components/schemas/SignInRequest.yaml'

responses:
  '200':
    description: 'Successfully logged in a user!'
    content:
      application/json:
        schema:
          type: object
          required:
            - status
            - message
            - data
          properties:
            status:
              type: integer
              example: 200
            message:
              type: string
              example: 'Successfully logged in a user!'
            data:
              type: object
              required:
                - user
                - accessToken
              properties:
                user:
                  $ref: '../../components/schemas/SignInResponse.yaml'
                accessToken:
                  type: string
                  example: ydRUQDKZCi2Tc0MJUFcOKANMa1MM4xr3XcYlK5V5

  '401':
    $ref: '../../components/responses/401.yaml'
